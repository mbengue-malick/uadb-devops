pipeline {
    agent {
        dockerContainer {
        
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'docker --version'
            }
        }
    }
}